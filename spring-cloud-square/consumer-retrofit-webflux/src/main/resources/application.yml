spring:
  application:
    name: square-consumer-retrofit-webflux
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true
      host: livk.com

server:
  port: 8774
