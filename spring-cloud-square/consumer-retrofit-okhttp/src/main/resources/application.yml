spring:
  application:
    name: square-consumer-retrofit-okhttp
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true
      host: livk.com

server:
  port: 8773
